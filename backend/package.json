{
  "name": "dhan-sarthi-backend",
  "version": "1.0.0",
  "description": "Backend API for Dhan-Sarthi Personal Finance App",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd .. && npm run build && cd backend && mkdir -p dist && cp -r ../dist/* dist/",
    "build:windows": "cd .. && npm run build && cd backend && if not exist dist mkdir dist && xcopy /E /I /Y ..\\dist dist",
    "serve": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["finance", "api", "express", "mongodb"],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "express-validator": "^7.0.1",
    "helmet": "^7.0.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}